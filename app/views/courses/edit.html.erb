<h2>Kurssin muokkaus</h2>

<% form_for(@course) do |f| %>

<%= render :partial => "form", :locals  => {:f => f } %>
   <p>
    <%= f.submit 'Päivitä' %>
  </p>
<% end %>

<%= link_to 'Show', @course %> |
<%= link_to 'Back', courses_path %>